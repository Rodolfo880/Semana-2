"""Codigo principal del programa, se define la función main() que es el punto de entrada
#del programa. Si el archivo se ejecuta directamente, se llama a la función main(). 
#Esto es útil paraorganizar el código y evitar que ciertas partes se ejecuten cuando el
#archivo es importado como un módulo en otro programa"""
def main ():
  # pass, si no hay elementos en la función, se puede usar el comando pass para evitar errores
    print("Hola Mundo")

if __name__ == "__main__":   # Si el archivo se ejecuta directamente, se llama a la
    main()                   # función main()